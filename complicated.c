int fact(int x)
{ 
    int a;
    int b;
    void c;
    
    if ( (x + 3) * fact(3 + 5, fact(9 / 1)) < (5 - 8) / (3 * 2) ) {
      if ((8 - 1) > 5) {
        while(fact(3-5/2) > 0)
           return fact(x);
      } else {
        return fact((a - b) / 5, fact(8/1) * b, c);
      }
    } else {
      fact(a, b, c);
    }
}
